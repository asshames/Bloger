import React from "react";
import classes from "./FullPost.module.css";

const FullPost = (props) => {
    return (
        <div className={classes.FullPost}>
            <p>Please Open a post</p>
        </div>
    );
};

export default FullPost;